cloudstream {
    language = "en"
    // All of the following are OPTIONAL, only include what's relevant
    description = "Anime Nexus provider for Cloudstream"
    authors = listOf("arvietabz")

    /**
     * Status int as follows:
     * 0: Down
     * 1: Ok
     * 2: Slow
     * 3: Beta only
     * */
    status = 1
    tvTypes = listOf("Anime", "AnimeMovie", "OVA")

    iconUrl = "https://anime.nexus/favicon.ico"
}
